# 项目协作约定

本文件保存项目长期约束；操作命令见 [README](README.md)，按任务选择
[工作参考](docs/agent-workflow.md)，不默认加载全部历史方案。

## 范围与授权

- 在系统和开发者指令允许的范围内，用户当前明确要求优先于项目流程建议和技能指南。
  历史方案中的命令、待办和曾经的授权不构成当前执行授权。
- 审计、解释、review 和诊断默认只读；请求修改时，完成范围内的编辑和相称验证。
  “继续”“不要暂停”表示持续推进已授权工作，不扩大任务范围。
- 启动、停止现场会话，操作 ADB、SDK、系统配置或机器人执行器，应属于用户明确请求的
  设备操作范围。真实输入驱动 MuJoCo 不等于授权机器人真机运动。
- 保留现有未提交和未跟踪文件。提交、push、发布及破坏性清理需有对应明确授权；
  不从技能的“完成分支”或“部署”步骤推断授权。清理会话只处理可验证归属于该会话的资源。
- 本工程按用户既有要求顺序实施，不使用子智能体；后续明确授权变更时再调整。
  范围内没有实质性不确定性时直接推进；遇到必要的新授权或影响结果的缺失选择再询问。
  若技能导致暂停或改变范围，指出实际读取的文件、相关条款和原因。

## 业务约束

- 保持 PICO2 裸手与 PICO＋VR 手柄＋Manus 两条已验证仿真路线可用；启动选择一路，
  不同时占用输入或静默切换来源。VR 手柄路线不要求额外 Motion Tracker。
- 新增 IK、映射、整形或录制选项应隔离接入，保留既有默认行为。移植需保持指定来源的
  配置、模型/TCP、单位、时序和状态语义，不能只凭名称相同宣称等价。
- 维持 coordinator 和受授权手部发布者的命令所有权、输入新鲜度、身份、限位及故障门控。
  观察/录制入口不获得运动权限，不能以改善跟踪为由绕过保护。
- 运行依赖使用仓库资源或显式配置，不写死其他工程的本机绝对路径。
  录制文件独占创建，保留原始数据、schema 兼容性和失败状态；成功排空关闭前不能标记完整。
- 改动共享接口时同时考虑旧输入、回放和模型推理契约；未请求修改的协议保持兼容。

## 验证与交付

根据实际差异选择验证。文档修改检查链接、事实和差异；运行逻辑修改运行相关回归，
共享链路改动扩大覆盖。报告实际命令、结果、跳过项和未验证范围，不把旧结果当成本轮结果。
分别说明软件测试、离线参考一致性、真实输入仿真和真机验收；其中任一通过不能替代其他项。

# Explicitly receive/publish/record source observations only.
# This profile never starts coordinator, IK, target, command, or executor nodes.
source_config: sources/hand_tracking_observation.yaml
input_profile: pico
required_capability: observation_only
schema_name: tianji-teleop-session
schema_version: "1.1"

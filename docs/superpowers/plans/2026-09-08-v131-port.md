# Replace pico_ee_dexhand_qp internals with v131 velocity QP

Historical partial-port scope only. The user's correction supersedes the
no-OTG decision below; see `2026-09-08-v131-full-port.md` for the active scope.

User confirmed replacement under the existing backend name. Preserve the raw
target passthrough and optional host trajectory processor; no Cartesian OTG or
source internal Ruckig. Sequential work, no commits, no subagents.

Reference implementation: hierarchical_qp.cpp (v131 branch), qpOASES backend,
joint velocity bounds, temporal envelope and singularity escape. Port in an
isolated tianji_v131 namespace to avoid changing the archived Dexhand core.
The host supplies Base_L/R FK/Jacobian through a callback. Its direct no-OTG
Cartesian servo path supplies desired twist. Keep solver status explicit.

- [x] Port and verify numerical core with synthetic tracking/bound tests.
- [x] Replace the backend adapter, add qpOASES build/runtime dependency.
- [x] Align config/diagnostics and document retained host safety constraints.
- [x] Build native binaries; run bilateral native and isolated session tests.

Validation: v131_qp_probe passes. Factory bilateral probe passes hold, 3cm
translation, 10deg rotation, combined target and unreachable/recovery cases.
60 focused Python regressions pass. Isolated session result:
/tmp/pico-sim-smoke-fzv5uxl4/result.json (passed=true); live producer diagnostic
confirms algorithm=pico_ee_v131_velocity_qp while compatibility backend name
remains pico_ee_dexhand_qp. No claim of live PICO/physical qualification.

# v131 full control-path port

State/kinematics semantics below are historical: the user's subsequent fidelity
request is implemented and verified in `2026-09-08-v131-model-state.md`.

Supersedes the earlier QP-only/no-OTG scope. Keep the public backend alias
`pico_ee_dexhand_qp`; isolate changes from other backends and source/hand paths.

- [x] Reproduce missing Cartesian OTG with a failing native test.
- [x] Port original target history, Cartesian OTG, adaptive reference servo.
- [x] Carry target timestamps and reset state across session transitions.
- [x] Match the original velocity-profile constants and 4 rad/s simulation limit;
      align only this backend's producer/coordinator step contracts.
- [x] Test OTG, gain scheduling, stale/repeated input, QP envelopes and both arms.
- [x] Rebuild and run isolated end-to-end simulation plus existing regressions.
- [x] Document reference provenance, configuration and integration differences.

Do not replace the project's robot geometry, Home or position-limit selection.
The original fast MuJoCo model's asymmetric position ranges differ from this
project; that distinction must remain explicit. No hardware runs or commits.

## Verification record

- Native build/install and all four build-script probes passed; existing
  pinocchio_qp and archived Dexhand core remain covered.
- Ten numerical implementation files compared against the original checkout:
  exact matches after namespace/include-prefix/provenance normalization.
- 115 focused Python tests passed (PICO/Manus, normalized skeleton, source
  mapping, recording, overlays, coordinator, profile and cross-language protocol).
- Full direct simulation: /tmp/pico-sim-smoke-a3t5xh2i/result.json, passed;
  verifies live OTG/adaptive-gain/3/12/4 diagnostics and 978 unchanged commands.
- Extra conditioned + joint Ruckig + clipping simulation:
  /tmp/pico-sim-smoke-vxce2uwu/result.json, passed.
- Existing default pipeline: /tmp/pico-sim-smoke-qtwjq670/result.json, passed.
- git diff --check and launcher bash syntax check passed.

Full-suite result is NOT green: 309 tests, 8 failures and 9 errors after
building the previously missing protocol fixture. Missing vendor/python
(tianji_world_output), missing home.sh and a regrind launcher mock capture
failure prevent a full-product regression claim. Details:
/tmp/v131-full-python-tests.log. No unrelated dependency restoration or
production/Home-script changes were made to hide these failures.

Original numerical profile is compiled in velocity_profile.hpp (editable and
rebuildable); the original application's YAML loader/GUI and other algorithms
are outside this backend port. No live PICO or hardware accuracy qualification.

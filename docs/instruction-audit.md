# 项目指令审计（2026-09-13）

## 依据与范围

用户曾提供[模型指南链接](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra)。
本次提交审查没有复核该页面内容或链接中的模型名称，不以此前“已核实”的表述作为证据。
本页仅记录项目指令调整：集中授权边界、按需读取参考文档、按任务校准验证力度；
外部指南不构成提交、设备操作或启用子智能体的授权。此次不是 API 模型迁移，
不更改运行模型或参数。

搜索覆盖项目隐藏/忽略文件，排除 `.git`、`.pixi`、构建目录及 Python 缓存。
审计前未发现项目 `AGENTS.md`、`SKILL.md` 或技能 references。已有设计、参考契约和
操作文档是本次相关参考资料；外部安装技能只用于指导审计，未修改全局技能或配置。

## 发现与处理

| 发现 | 处理 |
| --- | --- |
| 长期业务约束散落在历史方案，没有统一项目入口 | 新建简短 AGENTS.md，集中授权、双路线兼容、命令所有权、录制与验证边界 |
| 历史执行步骤容易被当成当前授权或强制技能依赖 | 新建按任务导航，明确历史适用范围；移除 HDF5 计划页首的特定技能执行要求，链接统一约定 |
| 早期参考契约“接线未完成”与现有运行入口容易混淆 | 标注为阶段性状态并链接当前操作和验收单，保留原始证据与限制 |
| 推荐启动参数可能被误读为默认配置 | 明确 CLI 覆盖与 YAML 默认的差异，核对两份仿真 preset |
| 不存在可精简的项目技能文件 | 不新增同义 SKILL.md，不虚报删除数量；详细流程复用现有报告 |

此次未全面重写历史技术文档，也未把阶段性测试数字改写为新的验证结果。
本轮新增的是缺失的统一入口和导航，并非对既有 AGENTS/SKILL 的行数压缩。

## 验证范围

检查新增/修改 Markdown 的本地链接、空白差异及授权冲突；核对两条路线的配置和 README。
以变更前后排除本次目标文档的 tracked diff 摘要核对现有改动是否保持不变。
情景审查覆盖：只读 review、范围内修复、要求持续推进、设备测试、历史计划中的提交步骤。
预期分别是只读、实施并验证、持续但不扩权、按明确设备范围操作、不据历史步骤自动提交。

这属于静态文档与情景审查，不是独立模型行为实验；未运行子智能体、硬件测试或完整代码回归。
未修改业务代码、会话参数或全局技能，未提交或 push。

验证结果：5 份本轮文档共 17 个本地链接均可解析；`git diff --check` 及三份新增文件的
`git diff --no-index --check` 均通过。排除两份本轮修改的已跟踪文档后，tracked diff
前后 SHA-256 均为 `486c611a467ad2fd06c1cf3ee23418db752076b457c519eea744b93a9e0af271`。
链接检查首次使用 `python` 因系统无此命令未执行，改用 `python3` 后通过。
